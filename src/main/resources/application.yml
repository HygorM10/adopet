spring:
  datasource:
    username: root
    url: jdbc:mysql://localhost:3306/adopets?createDatabaseIfNotExist=true&serverTimezone=UTC&useSSL=false
    password: ''
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: 'update'